<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhao.mapper.UserMapper">
    <select id="getPassword" parameterType="String" resultType="String">
        SELECT password
        FROM user_test
        WHERE username = #{username}
    </select>

    <select id="getRole" parameterType="String" resultType="String">
        SELECT role
        FROM user_test
        WHERE username = #{username}
    </select>

    <update id="updatePassword" parameterType="String">
        UPDATE user_test
        SET password = #{password}
        WHERE username = #{username}
    </update>

    <select id="getUser" resultType="com.zhao.model.User">
        SELECT *
        FROM user_test
        WHERE username = #{username}
    </select>

    <update id="banUser" parameterType="String">
        UPDATE user_test
        SET ban = 1
        WHERE username = #{username}
    </update>

    <select id="checkUserBanStatus" parameterType="String" resultType="int">
        SELECT ban
        FROM user_test
        WHERE username = #{username}
    </select>

    <select id="getRolePermission" parameterType="String" resultType="String">
        SELECT role_test.permission
        FROM user_test, role_test
        WHERE username = #{username} AND role_test.role = user_test.role
    </select>

    <select id="getPermission" parameterType="String" resultType="String">
        SELECT permission
        FROM user_test
        WHERE username = #{username}
    </select>
    <select id="getRoleList" resultType="java.lang.String">
        SELECT role
        FROM user_test
        WHERE username = #{username}
    </select>
    <select id="getPermissionList" resultType="java.lang.String">
        SELECT permission
        FROM user_test
        WHERE username = #{username}
    </select>
</mapper>