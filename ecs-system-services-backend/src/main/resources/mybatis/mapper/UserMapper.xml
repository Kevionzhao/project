<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zdxf.sysmanage.mapper.UserMapper">
    <insert id="insertUser" parameterType="com.zdxf.sysmanage.User" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user (username, password,updatetime,status) VALUES (#{username},
        #{password},#{updatetime},#{status});
    </insert>
    <select id="findByUsername" parameterType="java.lang.String" resultType="com.zdxf.sysmanage.User">
        SELECT * from sys_user where user_name = #{username};
    </select>
</mapper>